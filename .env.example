# Copy this file to .env and fill in the values you need.
# Only secrets belong here; non-secret defaults live in config/default.yaml.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
THAILLM_API_KEY=

# Ollama runs locally and needs no API key; override the host if not default.
OLLAMA_HOST=http://localhost:11434
